[package]
name = "flux-text"
description = "Safe Rust bindings to flux-text: HarfBuzz shaping over flux's glyph-blit primitive"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
flux = { path = "../flux", version = "0.0.7" }
flux-text-sys = { path = "../flux-text-sys", version = "0.0.7" }
