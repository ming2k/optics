# Capability & Scenario Demos (Tier 2 Architecture).
#
# Unlike micro-starters, scenarios are multi-file, production-like application
# slices demonstrating real capabilities (overlays, workspace, controls, tables).

subdir('overlays')
