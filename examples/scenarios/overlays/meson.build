# Overlays scenario — multi-file modular capability showcase.

executable('scenario_overlays',
  'main.c',
  'menus.c',
  'modal.c',
  dependencies        : [iris_dep, lens_dep, flux_dep, m_dep],
  include_directories : [iris_inc, lens_inc],
)
