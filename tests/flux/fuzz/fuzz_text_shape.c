/* fuzz_text_shape.c — placeholder, real harness lands when a deviceless
 * BiDi / itemize entry point is exposed (see flux_text_layout crate). */

#include <stddef.h>
#include <stdint.h>

int LLVMFuzzerTestOneInput(const uint8_t *data, size_t size) {
    (void)data;
    (void)size;
    return 0;
}
